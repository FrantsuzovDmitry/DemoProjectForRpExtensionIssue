1. Fill in the `ReportPortal.config.json` file with a valid server address, token, etc. Set the `enabled` flag to true.
2. In App.config, set the startup mode to Smoke.
3. Run the test marked with the `@NoSmoke` tag.
